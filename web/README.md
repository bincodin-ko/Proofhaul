# 운임근거함 (본체)

운송 한 건에 붙는 증빙을 **요청하고 · 받고 · 빠뜨린 걸 찾는다.** 사양은 `docs/01-SPEC.md`.

`bait/`(Day 0 미끼 도구)와 코드를 공유하지 않는다. 별개의 앱이다.

## 지금까지 된 것 — A1 ~ A4

`docs/06-FAST-TRACK.md`의 Phase A 중 **A1(골격과 규칙 인터페이스)**,
**A2(인증과 테넌트 분리)**, **A3(화면 1 — 운송 건 등록)**,
**A4(화면 2 — 토큰 링크와 서명)**.

- Next.js(App Router) + Postgres 골격
- `docs/01-SPEC.md` 데이터 모델대로 스키마와 마이그레이션
- 테넌트 분리 — RLS + 앱 단일 진입점
- `rules/` 인터페이스. **숫자는 하나도 없다**
- 로그인 (이메일 + 비밀번호, 소셜 없음), 세션, 속도 제한
- 화면 1 — 운송 건 등록. 단건 입력 + **엑셀 붙여넣기 다건 등록**
- 화면 2 — 증빙 요청(토큰 링크)과 **로그인 없는 모바일 서명**

화면 3은 A6에서 붙는다.

## 돌리기

```
cp .env.example .env        # .env는 커밋하지 않는다
npm install
npm run migrate
npx tsx scripts/create-company.ts "회사 이름" you@example.com   # 첫 계정
npm test
npm run dev
```

`npm test`는 실제 Postgres에 붙어서 돈다. `DATABASE_URL`과 `DATABASE_URL_APP`이 필요하다.

### 브라우저 검증

```
npm run dev        # 한 터미널
npm run e2e        # 다른 터미널
```

`tests/e2e/`는 단위 테스트가 못 보는 것을 본다 — 쿠키 플래그, 화면에 실제로 찍힌
글자, 주소를 직접 쳤을 때의 응답, 붙여넣기, 손가락 서명. 문서가 "실제로 시도해서
확인하라"고 한 항목의 근거다. 검증용 회사 2곳을 새로 만들고 끝나면 지운다.

**임시 폴더가 아니라 저장소에 둔다. 재현할 수 없는 확인은 확인이 아니다.**

셀프 가입은 아직 없다. 계정은 `scripts/create-company.ts`로 만든다. 비밀번호는
인자로 받지 않고 임의로 만들어 한 번만 출력한다 — 명령행 인자는 셸 기록과
프로세스 목록에 남는다.

## 규칙은 모양만 있고 값은 비어 있다

```ts
rulesFor("2026-07-31")?.version   // "2026.02"
rulesFor("2026-08-01")?.version   // "2026.08"   ← 7월 건과 8월 건의 기준이 다르다
rulesFor("2026-01-31")            // null        ← 확정된 버전이 없는 날짜
```

`waitThreshold` · `surcharge` · `fuelAdjustment`는 **전부 `null`을 돌려준다.**
국토교통부 고시 원문 대조가 끝나지 않았다(`docs/02-RULES-v2026.md` B절).

- `null`은 "미확정"이다. **0이 아니다.** 화면과 PDF는 `null`을 받으면 "미지원"을 띄운다
- `?? 0`을 쓰고 싶어지면 `requireSupported()`를 쓴다. 조용히 0으로 떨어지는 대신 터진다
- 원문을 확보하면 `rules/v2026-*.ts`의 `null`만 바꾼다. 앱 코드는 건드리지 않는다 (Phase B2)

`tests/rules.test.ts`의 첫 번째 테스트가 "모든 함수가 null을 돌려준다"를 검사한다.
원문 대조 전에 누가 값을 채워 넣으면 여기서 터진다.

### 날짜는 KST로 본다

`2026-08-01 00:30 KST`는 UTC로는 7월 31일이다. UTC로 비교하면 8월 건이 7월 버전으로
계산된다. `toKstDate()`가 이걸 막는다.

## 테넌트 분리

여러 운송사가 한 DB를 쓴다. A 운송사가 B 운송사 데이터를 보면 끝이다
(`docs/05-SECURITY.md` 위협 2).

**두 겹으로 막는다.**

1. **DB — RLS.** 다섯 테이블 전부 `ENABLE` + `FORCE ROW LEVEL SECURITY`.
   정책은 `current_company_id()` 하나만 본다. 세션에 회사가 설정되지 않으면 `NULL`이고,
   `NULL` 비교는 거짓이라 아무 행도 보이지 않는다. **기본값이 차단이다**
2. **앱 — 단일 진입점.** 회사 데이터를 만지는 코드는 전부 `db/tenant.ts`의
   `withCompany()`를 거친다. 검사를 앱 곳곳에 흩어두면 하나만 빠져도 새어 나간다

런타임은 `proofhaul_app` 역할로 붙는다. 테이블 소유자가 아니고 `BYPASSRLS`도 없다.
설정 실수로 슈퍼유저가 들어가면 테넌트 분리가 통째로 무력화되는데 앱은 멀쩡히
동작해서 알아채지 못하므로, `assertAppRoleIsRestricted()`가 시작할 때 막는다.

`SET LOCAL`이라 트랜잭션이 끝나면 회사 맥락도 사라진다. 풀에서 다음 요청이 같은
커넥션을 받아도 앞 요청의 회사가 남지 않는다.

### 회사 맥락 없이 도는 두 경로

로그인과 토큰 서명은 회사를 알기 전에 일어난다. RLS를 끄는 대신 **필요한 것만
돌려주는 `SECURITY DEFINER` 함수 두 개**로만 뚫는다. 뚫린 구멍이 어디인지
`db/migrations/003_rls.sql` 하나만 보면 알 수 있게 했다.

| 함수 | 돌려주는 것 |
|---|---|
| `auth_lookup_user(email)` | 인증에 필요한 4개 칼럼만 |
| `evidence_by_token_hash(hash)` | 증빙 1건 + 서명 전 화면에 띄울 최소 정보 |

`evidence_by_token_hash`는 운송 건, 날짜, 품목, 요청 회사명까지만 돌려준다.
**차주 연락처·메모·출발지·다른 운송 건·금액은 나가지 않는다.** 테스트가 이걸 검사한다.

서명 쓰기는 A4에서 붙인다. 토큰으로 회사를 알아낸 뒤 그 회사 맥락에서 `withCompany`로
쓸지, 증빙 1건만 건드리는 정의자 함수를 하나 더 둘지는 A4에서 정한다.

## SPEC과 다른 곳 — 한 군데

`docs/01-SPEC.md`에서 `evidence`와 `fee_calc`는 `shipment`를 타고 올라가야 회사를 알 수 있다.
스키마에서는 **두 테이블에 `company_id`를 들고 있다.**

- 이유: RLS 정책마다 서브쿼리가 붙으면 정책 하나만 틀려도 새어 나간다.
  정책을 `company_id = current_company_id()` 한 줄로 유지하려는 것
- 일관성은 복합 외래키로 DB가 강제한다.
  `evidence (shipment_id, company_id) → shipment (id, company_id)`.
  **남의 회사 운송 건에 증빙을 달 수 없다.** 앱이 실수해도 DB가 거부한다
  (`tests/tenant.test.ts`에서 실제로 시도해서 확인)

나머지는 SPEC 그대로다.

## 스키마에 박아둔 것

- **모든 `id`는 UUID.** 자동증가 정수는 주소창 숫자만 바꿔도 남의 데이터가 열린다
- **`evidence.token_hash`** — 토큰 원문은 저장하지 않는다. DB 덤프가 유출돼도
  서명 링크를 복원할 수 없다. `UNIQUE`라서 토큰 1개는 증빙 1건에만 대응한다
- **`fee_calc.calc_version`** — 적용한 고시 시점. 과거 건은 최신 규칙이 아니라
  이 버전으로 재계산한다 (`docs/01-SPEC.md` "calc_version이 핵심이다")
- **`fee_calc.base_amount` / `total_amount`는 `NULL` 허용.** `NULL`이 미지원이고 0이 아니다
- `evidence.signer_ip` / `signer_user_agent` — 분쟁 대비. 개인정보이므로
  보관 기간 대상이다 (A7에서 파기 스크립트)

## 검증한 것

`tests/tenant.test.ts` — **실제 Postgres에 붙어서** 회사 2곳을 만들고 남의 데이터에
닿는지 시도한다. 코드를 읽는 건 검증이 아니다 (`docs/05-SECURITY.md` 위협 2).

- 남의 `shipment` · `evidence` · `app_user` · `company`를 id로 직접 조회 → 0건
- 남의 데이터 수정·삭제 → 0건 (소유자 눈으로 원본이 그대로인지 재확인)
- 남의 `company_id`로 삽입 → RLS 위반으로 거부
- 남의 운송 건에 증빙 달기 → 외래키 위반으로 거부
- 회사 맥락 없이 조회 → 0건
- 토큰 문자열 1글자 변조 → 0건
- 토큰 조회 응답에 연락처·메모가 섞여 있지 않은지
- 다섯 테이블 `FORCE ROW LEVEL SECURITY` 확인, 다섯 테이블 `id` 칼럼 타입이 `uuid`인지 확인

`tests/rules.test.ts` — 규칙 함수가 전부 `null`인지, 버전 선택이 KST 기준으로 맞는지.

## 로그인 — A2

`docs/05-SECURITY.md` 위협 7이 요구하는 네 가지를 `lib/auth.ts` 한 곳에 모았다.

| 요구 | 어떻게 |
|---|---|
| argon2 | `@node-rs/argon2`, Argon2id. 직접 만든 해시 없음 |
| 세션 쿠키 | `httpOnly`, `sameSite=lax`, 프로덕션에서 `secure` |
| 속도 제한 | 15분 창에 IP 20회 / 이메일 10회 실패면 검증 자체를 하지 않는다 |
| 응답 통일 | 없는 계정과 틀린 비밀번호가 **같은 문구**로 나간다 |

문구만 통일하면 부족하다. 계정이 없을 때 바로 돌려주면 **응답 시간**이 존재 여부를
알려준다. 그래서 없는 계정에도 더미 해시로 같은 비용을 치른다 (`burnVerifyTime`).

속도 제한에 걸리면 **맞는 비밀번호도 거부한다.** 제한 상태에서 성공/실패가
갈리면 그게 곧 계정 확인 수단이 된다.

세션 토큰도 증빙 토큰과 같은 규칙이다 — 쿠키에는 임의 32바이트, DB에는 해시만.
로그아웃은 쿠키만 지우지 않고 DB의 세션 행도 지운다. 쿠키가 어딘가 복사돼 있어도
그 순간부터 못 쓴다.

로그인 시도 기록(`login_attempt`)에는 이메일을 평문으로 남기지 않는다. 실패 로그가
그대로 이메일 목록이 되면 안 된다.

**비밀번호 재설정은 아직 없다.** 메일 발송 수단이 없어서다(A7에서 어댑터를 붙인다).
붙일 때 위의 `LOGIN_FAILED_MESSAGE`를 그대로 쓴다 — 재설정에서도 계정 존재 여부가
드러나면 안 된다.

## 검증한 것 — A2

`tests/auth.test.ts` (13건) — 실제 DB에 붙어서.

- 없는 계정과 틀린 비밀번호의 반환값이 **같은 객체**인지
- 없는 계정의 응답 시간이 눈에 띄게 빠르지 않은지
- 이메일 10회 / IP 20회 실패 후 **맞는 비밀번호도 거부**되는지, 다른 계정은 멀쩡한지
- 세션 토큰이 DB에 해시로만 있는지, 원문이 어느 칼럼에도 없는지
- 만료된 세션이 열리지 않고 그 자리에서 지워지는지
- 토큰 한 글자 변조 → 안 열림
- 시도 기록에 이메일 평문이 없고 지문으로만 찾아지는지

**브라우저로 계정 2개를 만들어 실제 침범을 시도했다** (13건, 전부 차단).

- 로그인 없이 `/` → `/login`으로
- 틀린 비밀번호와 없는 계정의 **화면 문구가 글자까지 동일**
- 세션 쿠키 `httpOnly` · `sameSite=Lax`, `document.cookie`로 읽히지 않음
- A의 화면에 B의 회사 이름이 없음
- 토큰 한 글자 변조 → 로그인 화면
- 임의 토큰 → 로그인 화면
- **B의 토큰을 들고 들어가면 B만 열린다.** A의 데이터가 따라오지 않는다
- 로그아웃 뒤에는, 그 전에 복사해 둔 쿠키도 통하지 않는다

`secure` 플래그는 개발 서버(http)라 `false`로 확인됐다. 프로덕션에서는 `true`다.

## 화면 1 — A3

**엑셀 붙여넣기가 이 화면의 핵심이다.** 이 업종은 엑셀에서 온다. 붙여넣기가 안 되면
안 쓴다 (`docs/01-SPEC.md` 화면 1).

스프레드시트가 셀 범위를 복사하면 클립보드에 TSV가 들어간다 — 셀은 탭, 줄은 CRLF,
탭·줄바꿈·큰따옴표가 든 셀만 큰따옴표로 감싸고 안의 큰따옴표는 둘로 겹친다.
`lib/paste.ts`가 이 형식을 그대로 읽는다. DOM도 DB도 건드리지 않는 순수 함수라
테스트할 수 있다.

- 첫 줄이 제목 줄인지 **짐작하고, 사용자가 뒤집을 수 있다.** 날짜처럼 보이는 칸이
  있으면 제목 줄이 아니라고 본다
- 칼럼 짝짓기는 제목 글자로 먼저, 안 되면 값의 생김새로 한 번 더. 드롭다운으로 고친다
- 미리보기에는 원본 글자가 아니라 **저장될 값**이 보인다. `2026.08.16` → `2026-08-16`,
  `01098765432` → `010-9876-5432`

### 짐작하지 않는 것

- **연도 없는 날짜(`8/15`)는 받지 않는다.** 올해로 짐작해서 넣으면 연말에 한 해가
  통째로 어긋난다
- **모르는 품목을 `기타`로 떨어뜨리지 않는다.** `냉동탑차`는 오류다
- 못 읽은 줄은 조용히 버리지 않고 **몇 번째 줄이 왜 안 되는지** 보여준다.
  나머지는 그대로 가져간다

한 번에 500건, 512KB까지 (`docs/05-SECURITY.md` 위협 5). 넘치면 잘랐다고 알려준다.
여러 건 저장은 한 트랜잭션이다 — 500건 중 300건만 들어간 상태로 끝나면 무엇이
들어갔는지 알 수 없다.

붙여넣기 화면에서 거른 초안을 서버가 다시 믿지 않는다. 서버 액션에서 한 번 더 본다.

## 검증한 것 — A3

`tests/paste.test.ts` (12건) — 스프레드시트 클립보드 형식 그대로 넣어서.
`tests/shipments` 대신 브라우저로 확인했다 (14건).

- 실제 **클립보드에 넣고 Ctrl+V**로 붙여넣어서 확인
- 제목 줄 자동 인식, 칼럼 8개 자동 짝짓기, 드롭다운으로 변경
- `냉동탑차`(모르는 품목)와 `8/18`(연도 없음) 2줄을 이유와 함께 걸러내고 나머지 3건만 저장
- 단건 저장 → 상세로 이동 → **증빙 요청 버튼이 바로 보인다**
- **B 계정으로 A의 운송 건 주소를 직접 열면 404** (RLS가 걸러서 "없는 것"과 구분되지 않는다)
- B의 목록에 A의 운송 건이 보이지 않는다

## 화면 2 — A4

이 제품에서 **가장 위험한 지점**이다. 로그인 없이 열리는 링크가 핵심 기능이라
공격 표면이 거기 몰려 있다 (`docs/05-SECURITY.md` 위협 1).

| 요구 | 어떻게 |
|---|---|
| CSPRNG 32바이트 | `randomBytes(32)`. base64url 43글자 |
| DB에는 해시만 | `evidence.token_hash`. 원문은 만든 순간 응답에만 있다 |
| 상수 시간 비교 | 조회 자체가 해시 일치지만, `timingSafeEqual`로 한 번 더 |
| 만료 7일 | `expires_at`. 만료 후에는 어떤 정보도 보여주지 않는다 |
| 토큰 1개 = 증빙 1건 | `token_hash`에 UNIQUE. DB 함수 서명이 증빙 1건만 건드린다 |
| 없는 토큰 = 만료된 토큰 | 같은 화면, 같은 문구 |
| IP 속도 제한 | 실패 분당 10회 / 전체 분당 60회 |
| 서명 후 수정 불가 | `UPDATE ... WHERE status='REQUESTED' AND expires_at > now()` |

### 서명 요청에 회사 맥락을 열어주지 않는다

서명하는 사람은 로그인이 없고 회사도 모른다. 그렇다고 그 요청에 회사 맥락을
통째로 열어 주면, 토큰 하나가 회사 전체의 열쇠가 된다.

대신 **토큰이 가리키는 증빙 한 건만 건드리는 DB 함수** `evidence_sign()`을 둔다.
갱신 조건(`REQUESTED` + 미만료)이 함수 안에 있어서, 이미 서명됐거나 만료된
토큰이면 0행이 갱신되고 앱은 그걸 실패로 받는다. **토큰 1개 = 증빙 1건이라는
규칙을 DB 함수 서명 자체가 지킨다.**

### 서명 전 화면에 띄우는 것

운송 건, 날짜, 품목, **요청 회사명**까지다. 차주 연락처·메모·출발지·화주명·
다른 운송 건·금액은 나가지 않는다. 토큰만 알면 누구나 보는 화면이다.

### 속도 제한을 둘로 나눈 이유

처음에는 분당 10회를 전부 셌는데, 그러면 **정상 서명자가 새로고침 몇 번으로 잠긴다.**
서명이 막히는 것은 이 제품에서 가장 나쁜 일이다. 막으려는 열거 공격은 실패로
나타나므로 실패를 좁게(분당 10회), 전체는 넉넉히(분당 60회) 둔다.

### 계산은 여전히 하지 않는다

대기시간 화면은 **시각만 기록한다.** `rules`의 `waitThreshold`가 `null`이면
초과 시간 자리에 "미지원"과 그 이유가 뜬다. 빈칸이나 0으로 두지 않는다.

**사진 첨부는 A5에서 붙는다.** 업로드는 내용 기반 타입 검사와 EXIF 제거가 같이
가야 해서 그쪽으로 미뤘다. 서명 이미지는 지금도 저장하지만, 저장 위치를
**어떤 경로로도 서빙하지 않는다** — URL만 알면 열리는 상태를 만들지 않으려는 것이다.

**SMS 발송은 A7**(발송 어댑터)에서 붙는다. 지금은 링크 복사만 된다.

## 검증한 것 — A4

`tests/token.test.ts` (13건) + 브라우저 (22건). 실제로 시도해서 확인했다.

토큰 문자열을 다섯 가지로 바꿔 다른 증빙에 접근을 시도했고 **전부 막혔다.**

| 시도 | 결과 |
|---|---|
| 끝 한 글자 바꿈 | 만료/오류 화면 |
| 앞 한 글자 바꿈 | 만료/오류 화면 |
| 한 글자 잘라냄 | 만료/오류 화면 |
| 대문자로 바꿈 | 만료/오류 화면 |
| 아예 없는 토큰 | 만료/오류 화면 |
| B의 토큰으로 열기 | **B의 증빙만** 열림. A의 회사·운송 건이 따라오지 않음 |

- 만료된 토큰과 없는 토큰의 화면이 **글자까지 같다**
- 서명 화면에 차주 연락처·메모·화주명이 **없다**
- 서명 후 같은 링크를 다시 열면 "이미 서명이 끝난 링크". 덮어쓰기 시도는 0행 갱신,
  먼저 저장된 서명자와 서명 이미지가 그대로 남는다
- 서명 시 `signer_ip`·`signer_user_agent`·입력값·서명 이미지가 모두 기록된다
- 실패가 몰리면 11번째에서 IP가 막힌다. 정상 토큰은 20번 새로고침해도 안 막힌다
- DB에도 조회 기록에도 원문 토큰이 없다
- 모바일(390px)에서 가로 스크롤 없음, 로그인 없이 열림

### 테스트가 잡은 버그

`date` 칼럼이 `Date` 객체로 와서 서명 화면이 `Sat Aug 15`를 들고 `rulesFor()`에
넘기고 있었다. 그대로 두면 서명 페이지가 터지거나 **8월 건이 7월 요율로 계산된다.**
`pg`의 DATE 파서를 꺼서 `'YYYY-MM-DD'` 문자열로 받는다 (`db/client.ts`).

## 점검에서 고친 것

A1~A4를 처음부터 다시 돌리면서 나온 것들이다.

1. **`login_attempt`·`token_lookup`의 id가 자동증가 정수였다.** "모든 id는 UUID.
   자동증가 정수 금지"를 어긴 자리다. 두 테이블은 주소창에도 API에도 나오지 않고
   RLS가 전부 막고 있어서 실제 위험은 없었지만, 예외를 두지 않는다 — "여기는
   괜찮다"가 쌓이면 규칙이 규칙이 아니게 된다. `007` 마이그레이션으로 UUID로 바꾸고,
   **어느 테이블도 자동증가 id를 쓰지 않는지 검사하는 테스트**를 박았다
2. **단건 입력은 연락처를 정리하지 않고 있었다.** 붙여넣기는 `01012345678`을
   `010-1234-5678`로 바꾸는데 단건 폼은 적힌 그대로 저장했다. 같은 번호가 경로에
   따라 다른 모양으로 저장되면 나중에 차주별로 묶을 때 갈라진다
3. **진짜 스프레드시트 클립보드가 가정과 달랐다.** LibreOffice Calc를 띄워 실제
   `.xlsx`에서 A1:H6을 복사해 붙여넣어 보니, 줄을 **LF**로 끊고 셀 안의 줄바꿈을
   따옴표 없이 **공백으로 펴서** 내보냈다 (윈도우 엑셀은 CRLF + 따옴표). 결과는
   같았지만 형식 가정이 반만 맞았다. 실제로 받은 클립보드를 `tests/paste.test.ts`의
   `CALC_PASTE`로 박아 두 형식을 모두 지킨다

## 아직 안 한 것

A5부터. 파일 업로드(사진 첨부)와 PDF, 화면 3(누락 목록), 개인정보 장치와
킬 스위치, SMS 발송 어댑터. 비밀번호 재설정과 셀프 가입도 아직이다.
